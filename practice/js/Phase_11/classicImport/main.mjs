import { Add as sum, Search } from './utils.mjs';
import Multiply from './utils.mjs';
import * as utils from './utils.mjs';


console.log(sum(2, 3));
console.log(Search(['apple', 'banana', 'cherry'], 'a'));
console.log(Multiply(2, 3));
console.log(utils.Add(3, 4));
